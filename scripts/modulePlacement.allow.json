{
  "encapsulation:src/createSigner/_common/zodSchemas.ts::src/transaction/_common/zodSchemas/transaction/transaction.ts": "Known deviation: TransactionIntentZodSchema is consumed only by createSigner, but it needs the action union that lives in transaction/_common. Closes when transaction/ exposes an entry that createSigner consumes instead of reaching in. See SKILL.md, Known deviation.",
  "encapsulation:src/createSigner/createExecuteTransaction.ts::src/transaction/_common/zodSchemas/transaction/transaction.ts": "Known deviation: TransactionIntentZodSchema is consumed only by createSigner, but it needs the action union that lives in transaction/_common. Closes when transaction/ exposes an entry that createSigner consumes instead of reaching in. See SKILL.md, Known deviation.",
  "encapsulation:src/createSigner/createSignTransaction.ts::src/transaction/_common/zodSchemas/transaction/transaction.ts": "Known deviation: TransactionIntentZodSchema is consumed only by createSigner, but it needs the action union that lives in transaction/_common. Closes when transaction/ exposes an entry that createSigner consumes instead of reaching in. See SKILL.md, Known deviation.",
  "placement:src/transaction/signTransaction/signTransaction.ts": "Known deviation: signTransaction/ is consumed by two createSigner executors, so the algorithm would move it under createSigner/. Closes with the transaction/ entry factory. See SKILL.md, Known deviation.",
  "placement:src/transaction/_common/zodSchemas/transaction/transaction.ts": "Known deviation: the fourth consumer is createSigner reaching in for TransactionIntentZodSchema. Closes with the transaction/ entry factory. See SKILL.md, Known deviation."
}
