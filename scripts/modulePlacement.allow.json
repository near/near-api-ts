{
  "encapsulation:src/createSigner/_common/zodSchemas.ts::src/transaction/_common/zodSchemas/transaction/transaction.ts": "Known deviation: TransactionIntentZodSchema is built from the action-shape schemas that live in transaction/_common. Closes when transaction/ exposes an entry that createSigner consumes instead of reaching in. See SKILL.md, Known deviation.",
  "placement:src/transaction/signTransaction/signTransaction.ts": "Known deviation: signTransaction/ is consumed by two createSigner executors, so the algorithm would move it under createSigner/. Closes with the transaction/ entry factory. See SKILL.md, Known deviation.",
  "placement:src/transaction/_common/zodSchemas/transaction/transaction.ts": "Known deviation: the outside consumer is createSigner/_common/zodSchemas.ts reaching in for the action-shape schemas. Closes with the transaction/ entry factory. See SKILL.md, Known deviation."
}
