{
  "encapsulation:src/createSigner/_common/zodSchemas.ts::src/transaction/signTransaction/zodSchemas/transaction/transaction.ts": "Known deviation: TransactionIntentZodSchema is built from the action-shape schemas that are private to signTransaction/. Closes when transaction/ exposes an entry that createSigner consumes instead of reaching in. See SKILL.md, Known deviation.",
  "placement:src/transaction/signTransaction/signTransaction.ts": "Known deviation: signTransaction/ is consumed by two createSigner executors, so the algorithm would move it under createSigner/. Closes with the transaction/ entry factory. See SKILL.md, Known deviation.",
  "placement:src/transaction/signTransaction/zodSchemas/transaction/transaction.ts": "Known deviation: placed for its one legitimate consumer, signTransaction.ts. The second consumer is createSigner/_common/zodSchemas.ts reaching in for the action-shape schemas, which drags the LCA out to src/. Closes with the transaction/ entry factory. See SKILL.md, Known deviation."
}
